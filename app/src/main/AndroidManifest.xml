<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApplication"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TestApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.map.MapActivity"/>

        <activity android:name=".ui.canvas.CoordinateActivity"/>

        <activity android:name=".ui.movebutton.MoveButtonActivity"/>

        <activity android:name=".ui.draw.DrawActivity"/>

        <activity android:name=".ui.filterimage.FilterImageActivity"/>

        <activity android:name=".ui.recycleviewtouch.RecycleViewTouchActivity"/>

        <activity android:name=".ui.recycleviewswipe.RecycleViewSwipeActivity"/>

        <activity android:name=".ui.recycleviewmultilevel.RecycleViewMultiLevelActivity"/>

        <activity android:name=".ui.searchtag.SearchTagActivity"/>

        <activity android:name=".ui.chooseandcropimage.ChooseAndCropImageActivity"/>

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:theme="@style/Theme.MyUCrop"
            tools:replace="android:theme" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAsIVtswkELFXpUs52b6AwTrtkdh2l0OPk"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>